@*@using TechBlogUI.Areas.Admin.Models*@ @*buna ihtiyacimiz kalmadi cunki Area-view icine ui viewdelo _ViewImports.cshtml 
kopyalayip import ettik*@ 

@model List<RoleAssignViewModel>

@{
    ViewData["Title"] = "AssignRole";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<h1>Assign Role</h1>

<form method="post">
    @for (int i = 0; i < Model.Count; i++)
    {
        <div class="form-check">
            <input type="hidden" asp-for="@Model[i].RoleId" />
            <input type="hidden" asp-for="@Model[i].Name" />
            <input type="checkbox" asp-for="@Model[i].Exist" class="form-check-input" />
            <label class="form-check-label" for="@Model[i].Exist">@Model[i].Name</label>
        </div>
    }
    <br />
    <button class="btn btn-info">Save Changes</button>
</form>
<hr />
<a href="/Admin/AdminRole/UserRoleList" class=" btn btn-primary">Back to List</a>

